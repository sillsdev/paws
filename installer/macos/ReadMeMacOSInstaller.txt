To produce the macOS .dmg installer:

Make sure the program has been compiled.

In a Terminal, edit BuildMacOS.sh and make sure have the java_path variable is set to the correct version of Java.

Invoke BuildMacOS.sh

Note:

You will need to change the following as appropriate:

In MacOSInstaller.sh, set the --app-version number and, if needed, copyright date.
